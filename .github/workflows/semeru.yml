name: Test Setup Java semeru PR#767

on: [push, pull_request]

jobs:
  test-setup-java:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up .tool-versions with ASDF format
        run: |
          echo "java semeru-jre-openj9-23+37_openj9-0.47.0" > .tool-versions
          cat .tool-versions

      - name: Test setup-java from PR branch
        uses: actions/setup-java@refs/pull/767/merge
        with:
          distribution: 'semeru'
          java-version-file: '.tool-versions'

      - name: Verify Java installation
        run: java -version